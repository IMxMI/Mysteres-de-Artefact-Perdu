package org.jeu;
/**
 * Enumération représentant les différentes sorties possibles dans le jeu.
 * Chaque sortie est associée à une direction (NORD, SUD, EST, OUEST) ou à une action de sortie (EXIT).
 */
public enum Sortie {
	NORD,SUD,EST,OUEST,EXIT;
}
